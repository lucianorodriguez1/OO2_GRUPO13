spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oo2_grupo13
    username: root
    password: toto30
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false